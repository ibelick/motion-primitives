export const metadata = {
  title: 'Spinning Text - Motion-Primitives',
  description: 'Easily animate text circularly.',
};

import { SpinningText } from '@/components/core/spinning-text';
import ComponentCodePreview from '@/components/website/component-code-preview';
import CodeBlock from '@/components/website/code-block';

# Spinning Text

Easily animate text circularly.

## Examples

### Basic Usage

<ComponentCodePreview
  component={
    <SpinningText
      radius={5}
      fontSize={1.2}
      className='font-medium leading-none'
    >
      {`pre-order • pre-order • pre-order • `}
    </SpinningText>
  }
  filePath='core/spinning-text'
  hasReTrigger
/>

### Custom Transition

<ComponentCodePreview
  component={
    <SpinningText
      radius={5}
      fontSize={1.2}
      duration={6}
      transition={{
        ease: 'easeInOut',
        repeat: Infinity,
        duration: 6,
      }}
      className=''
    >
      {`pre-order • pre-order • pre-order • `}
    </SpinningText>
  }
  filePath='core/spinning-text'
  hasReTrigger
/>

## Code

<CodeBlock filePath='components/core/spinning-text.tsx' />

## Component API

### SpinningText

| Prop      | Type            | Default | Description                                                                  |
| :-------- | :-------------- | :------ | :--------------------------------------------------------------------------- |
| children  | `ReactElement`  |         | The text content or component to be animated in a circular motion.           |
| width     | `number`        | `200`   | Defines the width and height of the circular text container.                 |
| textStyle | `CSSProperties` | `{}`    | Custom styles for the text elements or components inside the circle.         |
| degree    | `number`        | `180`   | The degree of rotation to apply to the text around the circle.               |
| duration  | `number`        | `10`    | The duration of the full circular rotation animation.                        |
| className | `string`        | `''`    | A custom class name to apply styles to each rotating character or component. |
