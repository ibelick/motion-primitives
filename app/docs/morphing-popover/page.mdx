export const metadata = {
  title: 'Morphing Popover - Motion-Primitives',
  description:
    'A popover that transforms from its trigger into the content using layout animations. It visually morphs instead of appearing as a separate element.',
};

import { MorphingPopoverBasic } from './morphing-popover-basic';
import { MorphingPopoverCustomTransitionVariants } from './morphing-popover-custom-transition-variants';
import ComponentCodePreview from '@/components/website/component-code-preview';
import CodeBlock from '@/components/website/code-block';

# Morphing Popover

A popover that transforms from its trigger into the content using layout animations. It visually morphs instead of appearing as a separate element.

## Examples

### Morphing popover basic

<ComponentCodePreview
  component={<MorphingPopoverBasic />}
  filePath='app/docs/morphing-popover/morphing-popover-basic.tsx'
/>

### Morphing popover with custom transition variants

You can pass a custom transition and variants to the popover with the `variants` and `transition` props.

<ComponentCodePreview
  component={<MorphingPopoverCustomTransitionVariants />}
  filePath='app/docs/morphing-popover/morphing-popover-custom-transition-variants.tsx'
/>
