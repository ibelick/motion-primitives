export const metadata = {
  title: 'Resizable Card - Motion-Primitives',
  description:
    'A card that can be resized by dragging its edges. It can be used to create resizable panels or windows.',
};

import ResizableCardDemo from '@/components/core/resizable-card';
import ComponentCodePreview from '@/components/website/component-code-preview';

# Resizable Card

A card that can be resized by dragging its edges. It can be used to create resizable panels or windows.

<ComponentCodePreview
  component={<ResizableCardDemo />}
  filePath='core/resizable-card'
/>
