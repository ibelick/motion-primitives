import { AnimatedTabs } from "@/components/examples/animated-tabs";
import { AnimatedTabsHover } from "@/components/examples/animated-tabs-hover";
import { AnimatedCardBackgroundHover } from "@/components/examples/animated-card-background-hover";
import { SegmentedControl } from "@/components/examples/segmented-control";
import ComponentCodePreview from "@/components/website/component-code-preview";
import ComponentApi from "@/components/website/component-api";
import CodeBlock from "@/components/website/code-block";

# Animated Background

The `AnimatedBackground` component visually highlights selected items by sliding a background into view when hovered over or clicked. This smooth transition helps users focus on the active item, making it ideal for interactive lists, menus, navgiations where clear selection feedback is important.

## Examples

### Animated Tabs

<ComponentCodePreview
  component={<AnimatedTabs />}
  filePath="examples/animated-tabs"
/>

### Animated Tabs Hover

<ComponentCodePreview
  component={<AnimatedTabsHover />}
  filePath="examples/animated-tabs-hover"
/>

### Animated Card Background

<ComponentCodePreview
  component={<AnimatedCardBackgroundHover />}
  filePath="examples/animated-card-background-hover"
/>

### Segmented Control

<ComponentCodePreview
  component={<SegmentedControl />}
  filePath="examples/segmented-control"
/>

## Code

<CodeBlock filePath="components/core/animated-background.tsx" />

## Component API

<ComponentApi
  lines={[
    {
      prop: "children",
      type: `ReactElement<{ 'data-id': string }>[] | ReactElement<{ 'data-id': string }>`,
      default: "",
      description:
        "The content to be displayed within the animated background. Each child must have a unique `data-id` attribute to ensure correct functionality.",
    },
    {
      prop: "defaultValue",
      type: "string",
      default: "",
      description:
        "The default value to be used as the active item identifier.",
    },
    {
      prop: "onValueChange",
      type: "(newActiveId: string | null) => void",
      default: "",
      description:
        "Callback function that is called when the active item changes.",
    },
    {
      prop: "className",
      type: "string",
      default: "",
      description:
        "The class name to apply to the animated background for custom styling.",
    },
    {
      prop: "transition",
      type: "Transition",
      default: "",
      description:
        "The transition effect from framer-motion to apply when changing the active item.",
    },
    {
      prop: "enableHover",
      type: "boolean",
      default: "false",
      description:
        "Enables or disables the hover effect. When `enableHover` is true, spacing between children cannot be added.",
    },
  ]}
/>
