import { AnimatedTabs } from "@/components/examples/animated-tabs";
import { AnimatedTabsHover } from "@/components/examples/animated-tabs-hover";
import { AnimatedCardBackgroundHover } from "@/components/examples/animated-card-background-hover";
import { SegmentedControl } from "@/components/examples/segmented-control";
import ComponentCodePreview from "@/components/website/component-code-preview";

# Animated Background

The `AnimatedBackground` component visually highlights selected items by sliding a background into view when hovered over or clicked. This smooth transition helps users focus on the active item, making it ideal for interactive lists, menus, navgiations where clear selection feedback is important.

## Examples

### Animated Tabs

<ComponentCodePreview
  component={<AnimatedTabs />}
  filePath="examples/animated-tabs"
/>

### Animated Tabs Hover

<ComponentCodePreview
  component={<AnimatedTabsHover />}
  filePath="examples/animated-tabs-hover"
/>

### Animated Card Background

<ComponentCodePreview
  component={<AnimatedCardBackgroundHover />}
  filePath="examples/animated-card-background-hover"
/>

### Segmented Control

<ComponentCodePreview
  component={<SegmentedControl />}
  filePath="examples/segmented-control"
/>
