export const metadata = {
  title: 'Spotlight - Motion-Primitives',
  description:
    'A dynamic spotlight effect component that follows cursor movement.',
};

import { SpotlightBasic } from '@/components/examples/spotlight-basic';
import { SpotlightCustomColor } from '@/components/examples/spotlight-custom-color';
import { SpotlightBorder } from '@/components/examples/spotlight-border';
import ComponentCodePreview from '@/components/website/component-code-preview';
import CodeBlock from '@/components/website/code-block';

# Spotlight

A dynamic spotlight effect component that follows cursor movement.

## Examples

### Spotlight Basic 1

<ComponentCodePreview
  component={<SpotlightBasic />}
  filePath='examples/spotlight-basic'
  classNameComponentContainer='px-4'
/>

### Spotlight Custom Color

You can customize the color with using [Tailwind CSS gradient color stop](https://tailwindcss.com/docs/gradient-color-stops). (e.g `from-blue-800 via-blue-600 to-blue-400`)

<ComponentCodePreview
  component={<SpotlightCustomColor />}
  filePath='examples/spotlight-custom-color'
  classNameComponentContainer='px-4'
/>

### Spotlight Border

You can create a spotlight border by creating a container with a padding that will be used as a border.

<ComponentCodePreview
  component={<SpotlightBorder />}
  filePath='examples/spotlight-border'
  classNameComponentContainer='px-4'
/>

## Code

<CodeBlock filePath='components/core/spotlight.tsx' />

**Please add:**

- [cn](https://github.com/ibelick/motion-primitives/blob/main/lib/utils.ts)

## Component API

### Spotlight

| Prop          | Type          | Default         | Description                            |
| :------------ | :------------ | :-------------- | :------------------------------------- |
| className     | string        | ''              | Additional CSS classes for styling     |
| size          | number        | 200             | Size of the spotlight effect in pixels |
| springOptions | SpringOptions | `{ bounce: 0 }` | Animation spring configuration         |

## Credit

Initiated by [@divyanshu-vashishth](https://github.com/divyanshu-vashishth)
